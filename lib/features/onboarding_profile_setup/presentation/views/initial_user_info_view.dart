 import 'package:flutter/material.dart';

class InitialUserInfoView extends StatelessWidget {
  const InitialUserInfoView({super.key});
  static const String routeName = "initialUserInfoView";

  @override
  Widget build(BuildContext context) {
    return Center();
  }
}
